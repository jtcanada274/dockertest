pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                //sh 'node --version'
                script {
                    tail -f /dev/null
                }
            }
        }
    }
}
